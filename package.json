{
  "name": "minor_project",
  "version": "1.0.0",
  "description": "Laptop Website for minor project",
  "main": "server/index.js",
  "scripts": {
    "start": "cd client && npm run build && cd ../server && npm start",
    "dev": "cd client && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}

